// src/config/api.ts

/**
 * Base URL for the backend API.
 * In a real-world application, this would likely come from an environment variable.
 * e.g., const API_BASE = import.meta.env.VITE_API_URL;
 */
const API_BASE = 'http://localhost:8000/api';

export default API_BASE;
